# Talk to the FastAPI service directly rather than through the Next rewrite.
# A slow AOI fetch (reading 12 Sentinel bands off S3) outlives the dev proxy.
NEXT_PUBLIC_SATQUERY_API=http://127.0.0.1:8000

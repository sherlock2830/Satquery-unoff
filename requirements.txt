# Agent, service and inference. CPU only -- no CUDA anywhere in this file.
langgraph>=1.2
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-multipart>=0.0.9

torch>=2.5            # CPU wheel; see README for the --index-url
numpy>=1.26
pillow>=10.4
onnxruntime>=1.19

# IndiaSat build + the live Sentinel fetch
rasterio>=1.3
pyarrow>=17.0
